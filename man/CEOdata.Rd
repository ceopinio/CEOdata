% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CEOdata.R
\encoding{UTF-8}
\name{CEOdata}
\alias{CEOdata}
\title{Import datasets / microdata from the Centre d'Estudis d'Opinio}
\usage{
CEOdata(series = "BOP_presencial", reo = NA, raw = FALSE)
}
\arguments{
\item{series}{Character scalar identifying the accumulated series to download.
Ignored when \code{reo} is provided.}

\item{reo}{Single REO identifier of a study to download. Can be character,
numeric, or factor-like, and is normalized internally.}

\item{raw}{Logical. If FALSE (default), converts SPSS labelled vectors into
factors. If TRUE, returns raw haven-labelled vectors.}
}
\value{
A tibble with individuals' responses to the requested dataset.
}
\description{
Easy and convenient access to datasets / microdata from the
"Centre d'Estudis d'Opinio". The function can return either:
(1) an accumulated microdata series (identified by \code{series}), or
(2) a single study microdata dataset (identified by \code{reo}).
}
\details{
Accumulated series are obtained from the Dades Obertes catalogue and are
usually identified by \code{codi_serie} (e.g. \code{"BOP_presencial"}).
}
\examples{
\dontrun{
# Default: accumulated microdata series
d <- CEOdata()

# Load another accumulated series by code
d_tel <- CEOdata(series = "BOP_telefonica")

# Load a single study by REO
d1145 <- CEOdata(reo = "1145")
}
}
